package com.example.common.config;

import org.springframework.context.annotation.Configuration;

/**
 * MyBatis通用配置基类
 */
@Configuration
public class MyBatisConfig {
    // 通用MyBatis配置，子类可以通过继承并添加@MapperScan注解来指定扫描路径
} 